% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid_learners.R
\name{validate_lambda_grid}
\alias{validate_lambda_grid}
\title{Validate a User-Supplied Lambda Grid}
\usage{
validate_lambda_grid(lambda, learner_name)
}
\arguments{
\item{lambda}{The lambda argument passed by the user.}

\item{learner_name}{Character name used in error messages.}
}
\value{
The lambda grid, deduplicated and sorted in decreasing order (the
order in which pathwise coordinate descent proceeds).
}
\description{
Grid learners require an explicit, fixed lambda grid: if lambda were left
for the underlying package to choose, each cross-validation fold would
generate a \emph{different}, data-dependent lambda sequence, and the
"same-named" pseudo-learner would refer to different models in different
folds -- silently invalidating the cross-validated risk estimates used in
the meta-learning stage.
}
\keyword{internal}
